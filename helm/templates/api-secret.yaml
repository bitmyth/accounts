apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: {{ .Release.Namespace | quote }}
type: Opaque
stringData:
  plain.yaml: |
    server:
      port: 80
    locale: zn-CN
  secret.yaml: |-
    database:
      {{- .Values.mysql.auth | toYaml | nindent 6 }}
